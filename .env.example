# Docker 部署环境变量示例文件
# 复制此文件为 .env 并修改相应的值

# 管理员密码 (生产环境必须修改)
ADMIN_PASSWORD=your-secure-production-password

# JWT 密钥 (生产环境必须使用强密钥，至少32字符)
JWT_SECRET=your-very-long-and-random-secret-key-at-least-32-characters

# 数据库配置 (如果使用外部数据库)
# DATABASE_URL=your-database-connection-string

# 其他配置
NODE_ENV=production
